[package]
name = "markdown"

description = "A Notion-style block document model for gpui — markdown in, markdown out"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[features]
# The editing surface. It pulls no dependency the reader does not already
# carry, so the gate buys compile time and an API surface, not a lighter graph.
editor = []

[dependencies]
theme.workspace = true
gpui.workspace = true
pulldown-cmark = { version = "0.13", default-features = false }
